<% if @new == 0 %> 
  <% title "Mes nouveautés" %>
<% else %>
  <% title "(#{@new.count}) Mes nouveautés" %>
<% end %>
<div class="nav">
  <div class="nav-flex">
    <%= link_to garden_path do %>
      <div class="garden-button">
        <%= image_tag('garden_icon.svg') %>
      </div>
    <% end %>
    <%= link_to harvest_path do %>
      <div class="nav-green-button">
        <span>Ma récolte</span>
        <%= image_tag('white_arrow_right.svg', class: 'arrow-right') %>
      </div>
    <% end %>
  </div>
</div>
<div class="content-flex">
  <% if @fresh == [] && @new == [] %>
    <br><br>
    <h1>Vous êtes parfaitement à jour.<br>Pourquoi ne pas consulter votre récolte si vous en avez le temps ? 😊</h1>
  <% end %>
  <% unless @new == [] %>
    <%= render partial: 'shared/redbar', locals: {title: 'NOUVEAUX'} %>
    <% @new.each do |e| %>
      <% cache "entry-#{e.id}" do %>
        <%= render partial: 'shared/fruit', locals: {e: e} %>
      <% end %>
    <% end %>
    <%= render partial: 'shared/redbar', locals: {title: ''} %>
  <% end %>
  <% @fresh.each do |e| %>
    <% cache "entry-#{e.id}" do %>
      <%= render partial: 'shared/fruit', locals: {e: e} %>
    <% end %>
  <% end %>
</div>
<div class="go-to-harvest">
  <%= link_to harvest_path do %>
    <div class="column">
      <p>Today's harvest</p>
      <div class="row">
        <span class="normal-size harvest-count">0</span><p class="normal-size">&nbsp;added</p>
      </div>
    </div>
    <div class="column">
      <%= image_tag('chevron_right.svg', class: 'chevron') %>
    </div>
  <% end %>
</div>