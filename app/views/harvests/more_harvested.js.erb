$(".content").last().after("<%= escape_javascript(render partial: 'harvests/more_harvested', locals: { harvested: @harvested } ) %>"); 
if (<%= @load_more_count %> == 0) {
  $(".load-more-harvest").addClass("hidden");
}
else {
  $(".load-more-harvest").text("Voir <%= @load_more_count %> de plus");
}

$.fn.ButtonFunction();