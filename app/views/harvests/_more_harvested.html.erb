<% harvested.each do |h| %>
  <% if h.recommendation_id != nil %>
    <%= render partial: 'shared/recommendation', locals: {e: h.entry, r: h.recommendation} %>
  <% else %>
    <%= render partial: 'shared/fruit_harvested', locals: {e: h.entry} %>
  <% end %>
<% end %>