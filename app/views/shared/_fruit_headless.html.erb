<div class="content show">
  <div class="content-right-column">
    <div class="content-top">
      <div class="time-published source-show"><%= distance_of_time_in_words(e.created_at, Time.now) %> ago</div>
    </div>
    <div class="content-box">
      <%= render partial: 'shared/content_info', locals: {e: e} %>
      <%= render partial: 'shared/fruit_footer', locals: {entry: e} %>
    </div>
  </div>
  <div class="content-masked hidden">
    <span>You won’t see this content again.</span>
    <%= link_to entry_unmask_path(e), class: 'unmask', remote: :true do %>
      Unmask
    <% end %>
  </div>
  <div class="content-harvested hidden">
    <span>You have harvested this content.</span>
    <%= link_to entry_unharvest_path(e), class: 'cancel-harvest', remote: :true do %>
      Remove from harvest
    <% end %>
  </div>
</div>