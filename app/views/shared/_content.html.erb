<div class="content show">
  <div class="content-right-column">
    <div class="content-top">
      <div class="time-published source-show"><%= distance_of_time_in_words(e.created_at, Time.now) %> ago</div>
    </div>
    <div class="content-box">
      <div class="content-read" data-entry="<%= e.id %>">
        <%= link_to e.media_url, target: :blank, class: 'content-anchor' do %>
          <% if e.is_read_by_user?(current_user) %>
            <div class="read-sign">SEEN</div>
            <%= image_tag(e.thumbnail_url, class: 'content-thumbnail source-show low-opacity') %>
          <% else %>
            <div class="read-sign hidden">SEEN</div>
            <%= image_tag(e.thumbnail_url, class: 'content-thumbnail source-show') %>
          <% end %>
          <p class="content-title source-show"><%= e.title %></p>
        <% end %>
      </div>
      <p class="content-desc"><%= truncate(e.content, length: 200) %></p>
      <%= render partial: 'shared/fruit_footer', locals: {entry: e} %>
    </div>
  </div>
  <div class="content-masked hidden">
    <span>You won’t see this content again.</span>
    <%= link_to new_entry_masking_path(e), class: 'unmask', remote: :true do %>
      Unmask
    <% end %>
  </div>
  <div class="content-harvested hidden">
    <span>You have harvested this content.</span>
    <%= link_to new_entry_picking_path(e), class: 'cancel-harvest', remote: :true do %>
      Remove from harvest
    <% end %>
  </div>
</div>