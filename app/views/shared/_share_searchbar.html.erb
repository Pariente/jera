<div class="nav-search">
  <div class="nav-search-flex">
    <%= link_to harvest_path, class: 'margin-right-auto' do %>
      <div class="nav-search-back">
        <%= image_tag('white_arrow_left.svg', class: 'arrow-left') %>
        Retour
      </div>
    <% end %>
    <div class="nav-search-bar">
      <%= search_form_for @search, url: new_entry_recommendation_path(entry) do |f| %>
        <div class="magnifier">
          <%= image_tag('magnifier_icon.svg') %>
        </div>
        <%= f.search_field :username_has_any_term, class: 'friends_search_input', placeholder: 'Trouvez vos amis' %>
        <%= f.submit 'Chercher', class: 'friends_search_submit' %>
      <% end %>
    </div>
  </div>
</div>