<% title "Modifier mon profil" %>
<%= render partial: 'shared/navbar' %>
<div class="content-flex">
  <%= link_to destroy_user_session_path, method: :delete, class: "sign-out red underline" do %>Déconnexion<% end %>
  <h1>Modifier mon profil</h1>
  <hr>
<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= devise_error_messages! %>

  <div class="form-inputs">
    <label>Nom d'utilisateur</label>
    <%= f.input :username, autofocus: false, label: false %>
    <%= f.input :email %>
    <label>Photo de profil</label>
    <%= f.input :picture, label: false %>
    <label>Nouveau mdp (laissez blanc si inchangé)</label>
    <%= f.input :password, autocomplete: "off", label: false %>
    <br>
    <label class="brown bold">👉 Écrivez votre mdp actuel pour valider</label>
    <%= f.input :current_password, autocomplete: "off", label: false %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="form-actions">
    <%= f.submit "Modifier mon profil" %>
  </div>
<% end %>
  <p>Nous pensons que votre temps est votre trésor le plus précieux, et nous ne le respecterons toujours.</p>
  <p>Si nous avons échoué à cela, nous en sommes sincèrement désolés. Aussi, voici le lien pour supprimer votre compte :</p>
  <p class="center"><%= link_to "Supprimer mon compte", registration_path(resource_name), data: { confirm: "Êtes-vous bien sûr de vouloir supprimer votre compte ?" }, method: :delete, class: "red underline" %></p>
</div>